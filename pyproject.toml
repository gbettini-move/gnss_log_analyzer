[project]
name = "log_GNSS"
version = "1.0"
readme = "README.md"
requires-python = ">=3.10"

dependencies = [
    "numpy==1.25.2",
    "pandas==2.0.3",
    "plotly==6.5.2"
]

[dependency-groups]
dev = [
    "pytest",
    "ruff",
    "black",
]